{% extends "base.html.twig" %}



{% block title %}Liste des employées{% endblock %}

{% block body %}
    {% if app.user %}   
        <style>
            h1{ text-align: center; }
        </style>

        <div class="example-wrapper">
            <h1 style="background-color: grey; color: orange;">Employés d'Orange 🍊</h1>

            <section class = "employes">
                
                    <table class="table table-dark">
                        <thead>
                            <tr>
                            <th scope="col">#</th>
                            <th scope="col">Nom</th>
                            <th scope="col">Prénom</th>
                            <th scope="col">Age</th>
                            <th scope="col">adresse</th>
                            <th scope="col">Date Arrivé</th>
                            <th scope="col">Echelon</th>
                            <th scope="col">Service</th>
                            <th scope="col">Salaire</th>
                            <th scope="col">Congé Restant</th>
                            <th scope="col">Email</th>
                            </tr>
                        </thead>
                        {% for employe in employes %}
                            <tbody>
                                <tr>
                                <th scope="row">{{employe.id}}</th>
                                <td>{{employe.nom}}</td>
                                <td>{{employe.prenom}}</td>
                                <td>{{employe.age}}</td>
                                <td>{{employe.adresse}}</td>
                                <td>{{employe.dateArrive | date("m/d/Y")}}</td>
                                <td>{{employe.echelon}}</td>
                                <td>{{employe.service}}</td>
                                <td>{{employe.salaire}}</td>
                                <td>{{employe.congeRestant}}</td>
                                <td>{{employe.email}}</td>
                                </tr>
                                <a href='{{ path('supprimer_employe', {'id' : employes.id })}}' class = "btn btn-danger btn-sm">
                                <i class="fas fa-trash"></i>
                            </tbody>
                        {% endfor %}
                    </table>
                
            </section>

            <a href="{{ path('creer_employe') }}" class="btn btn-success"> Ajouter </a>

            {# <a href "{{ path('modifier_employe') }}" class="btn btn-warning"> Modifier </a> #}

            {# <a href "{{ path('supprimer_employe') }}" class="btn btn-danger"> Supprimer </a>
            #}
            
            {# <ul>
                <li>Your controller at <code><a href="{{ '/Users/phillipowusu/Desktop/LogicielRH/OrangeRh/src/Controller/OrangeController.php'|file_link(0) }}">src/Controller/OrangeController.php</a></code></li>
                <li>Your template at <code><a href="{{ '/Users/phillipowusu/Desktop/LogicielRH/OrangeRh/templates/orange/index.html.twig'|file_link(0) }}">templates/orange/index.html.twig</a></code></li>
            </ul> #}
        </div>
    

    {% else %}

        <h2 style = "position: absolute; margin: auto; top: 0; right: 0; bottom: 0; left: 0; width: 70%; height: 40%; text-align: center;"> Veuillez vous connecter !</h2>

    {% endif %}
{% endblock %}